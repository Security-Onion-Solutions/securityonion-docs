.. _so-checkin:

so-checkin
==========

``so-checkin`` will run ``salt-call state.highstate -l info`` to do a full update of all :ref:`salt` states. It outputs to the terminal with the log level set to ``info`` so that you can see exactly what's happening.
